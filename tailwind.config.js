// tailwind.config.js
export default {
  content: ['./src/**/*.{js,ts,jsx,tsx,css}'],
  // Optional: You don't need "theme.extend.fontFamily" if using CSS vars for fonts!
  // You may omit "theme" altogether or leave it empty if all theming is via CSS.
  plugins: [],
};